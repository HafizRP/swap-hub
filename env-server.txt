APP_NAME="Swap Hub"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database Configuration (Docker)
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=swap_hub
DB_USERNAME=root
DB_PASSWORD=nd+BHrttSQHGVhYRR7rCwgfqg8pRU6Nm697E/7/zsAQ=

# Session
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting & Queue
BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Cache
CACHE_STORE=database

# Redis (Docker)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Reverb Broadcasting (WebSocket) - Docker
REVERB_APP_ID=swap-hub
REVERB_APP_KEY=swap-hub-key
REVERB_APP_SECRET=swap-hub-secret
REVERB_HOST=reverb
REVERB_PORT=8080
REVERB_SCHEME=http

# Mail Configuration
# For development/testing, use 'log' to write emails to storage/logs/laravel.log
MAIL_MAILER=log
# For production, use 'smtp' and configure your mail service below
# MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@swaphub.com"
MAIL_FROM_NAME="${APP_NAME}"

# Or use log driver for testing
# MAIL_MAILER=log

# AWS (if needed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${APP_HOST:-localhost}"
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
